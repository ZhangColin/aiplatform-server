-- ========================================================================
-- #41 · business.project：「PRD 已产出」项目级状态位
-- PRD 事实源是项目 dev 工作区的 docs/PRD.md（工作区文件，非库表——PRD 是工作区的
-- 一部分，编码智能体直读，删除项目 = 工作区与 PRD 同亡，故不建文档表）。本列只记
-- 「BA 的 savePrd（#49）已成功写出过」这一门禁事实：G1 门谓词查本位不查文件系统。
-- NULL = 未产出；时间戳随每次写出刷新（产出/更新共用，v1 无版本链；版本链将来
-- 再加库投影）。
-- ========================================================================

ALTER TABLE prj_projects ADD COLUMN prd_produced_at TIMESTAMP;
