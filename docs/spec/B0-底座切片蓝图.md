# B0 · 底座重写切片蓝图

> **状态**：已定稿（2026-08-19，[wayfinder 票 #2](https://github.com/ZhangColin/aiplatform-server/issues/2) 产出）
>
> **双参考源**：`deepseek-harness/platform/`（demo，冻结的重写基准，Step 0-5.5 全链路已通）+ `aiplatform/Code-Canvas/`（同命题下更完整的实践：弱在引擎适配与知识库，强在数据模型、HITL/闸门语义、Checkpoint、计量配额、可靠性纪律与文档方法论）。两者是不同阶段、不同工具下的尝试，**都是重要参考源，都不照抄**。
>
> **执行方式**：每片各自走 grill→spec→ticket→implement→code review（地图协作约定）；本蓝图只定切片与验收，不写代码。

---

## 1. 包结构定稿

**单 module、双分区**。Maven 不拆模块；依赖方向靠 ArchUnit 守护：**`base` 包不得 import `business` 包**（一条规则）。

```
com.aieducenter.aiplatform
├── base                    # 分区：基础设施能力层（端口-适配器），零业务概念
│   ├── workspace           # BC：环境抽象 + Docker 后端 + 中间件资源 + 工作区落库   [SUPPORTING]
│   ├── agentengine         # BC：智能体适配层 + 引擎注册 + agent 会话落库          [GENERIC]
│   ├── eventhub            # BC：SSE 技术事件广播（内存单实例起步）               [GENERIC]
│   ├── knowledge           # BC：知识入库/检索 + EmbeddingClient + pgvector        [SUPPORTING]
│   └── process             # BC：阶段推进引擎（序列/门禁/迁移），无表，状态由持有方保存 [SUPPORTING]
└── business                # 分区：交付业务（CORE 子域）
    ├── project             # BC：项目聚合 + 角色卡 preset + 主链总装（B0 建最小版）
    ├── identity            # BC：账号 + OIDC BFF 认证（A2 建：idn_accounts + /auth/* + /api/me） [SUPPORTING]
    ├── workbench           # BC：门户读模型查询聚合——待办列表（A2 建；无表无领域实体，纯查询侧） [SUPPORTING]
                              # 远期 BC：task（A4）/ billing / asset / processconfig
```

- **分区 ≠ 限界上下文**。base/business 是架构标签（类比 domain/application 层名），BC 是区内每个包，各带 `package-info` + `@BoundedContext(subDomain=…)`，内部按 cartisan 规范四层（domain / application / infrastructure / endpoints）。
- 表前缀随 BC：`wsp_` / `agt_` / `knw_` / `prj_`（cartisan 表命名规范：同一上下文同前缀）。

**demo → 重写的两个关键拆解**（「底座零业务概念」的落地）：

| demo 现状 | 重写归属 | 理由 |
|---|---|---|
| `Role` 角色卡（enum 硬编码 systemPrompt + modelId）长在 agent 包 | **business.project**（代码配置起步，不落库） | docs/11：「业务智能体 MVP 用 agent preset」；`CodingAgentAdapter.runTask` 以 systemPrompt/modelId 为**入参**，适配层不含任何角色概念 |
| `Project` 聚合（业务字段+环境句柄+stage 混在一起）、`ProjectController` 是全局汇聚点 | **business.project**（聚合+主链 Controller）；环境句柄部分归 **base.workspace**（`wsp_workspaces` 落库，重启接回） | demo 的 `create` = 建工作区+落库+发事件，本质是业务编排；底座只管工作区生命周期 |
| 阶段→产物文件名映射（BA→PRD.md）在 KnowledgeService | **business.project** 调 `KnowledgePort` 时传内容+元数据 | 「哪个阶段产出什么文件」是业务知识 |
| Stage 状态机内嵌在 ProjectController.approve() | **base.process** 独立引擎类，无表；阶段序列（平台主链定义，A3：无模板概念）由业务侧传入 | docs/11：「Stage 状态机 = 流程引擎基础设施，业务期状态机映射其上」（A3 票） |

---

## 2. 六片切片

排序依据：依赖自底向上（workspace/eventhub 是叶子；agentengine、knowledge 依赖 workspace；project 总装依赖全部）。**片 3、片 4 互不依赖，可并行**。片 2 的事件载荷规格依赖地图「API 与 SSE 事件规范」票（#3）先定稿。

| # | 片 | 内容（demo 对应物重写为） | 新增表 | 验收口径（本机 8888） | 前端 issue |
|---|---|---|---|---|---|
| **0** | 工程基线 | Flyway 基线（`CREATE EXTENSION vector`）；ArchUnit 分区规则（`base` ↛ `business`）；CONTEXT.md 初始化 + CLAUDE.md 定位改写 | — | `mvn test` 绿；swagger `/swagger-ui/index` 可开 | 无 |
| **1** | `base.eventhub` + `base.workspace` | SSE 基座（`GET /api/events`）；`EnvironmentBackend` 端口 + `WorkspaceHandle`/`EnvKind`/`ExecResult` + Docker CLI 后端（ProcessBuilder，沿用 demo）+ 中间件资源供给（network/pg/redis 随环境，`.env` 注入）+ 工作区落库（重启接回） | `wsp_workspaces` · `wsp_resources` | curl 建工作区→容器+pg+redis 就绪、`.env` 注入、SSE `workspace-created`；exec 端点可跑命令；销毁级联清理（容器→网络→卷→库记录）；**服务重启后工作区记录仍在且可接回** | 无（REST 先就绪，对接并入片 5） |
| **2** | `base.agentengine` | `CodingAgentAdapter` 端口（runTask/pendingQuestions/replyQuestions/replyPermission/health；systemPrompt/modelId 入参）+ OpenCode/Dsh 适配器 + 引擎注册表（显式注册，不再靠 Spring bean 名）+ `AgentModelConfig` + agent 会话落库（按 workspaceId 寻址） | `agt_agent_sessions` | 对片 1 工作区下发任务→SSE 收全事件流（`task-start`→…→`task-finish`）；问答/权限端点可交互；**服务重启后会话可寻址** | 暂缓——spec 前先解决地图 #3（事件规范），约定定稿后随片 5 一起发 |
| **3** | `base.knowledge` | `KnowledgePort`（index(内容+元数据) / retrieve(query, topK)）+ `EmbeddingClient`（本机 fastembed `127.0.0.1:9091`，512 维）+ pgvector HNSW 余弦检索 | `knw_*`（实现片定名） | 喂文档分块入库；retrieve 语义命中；embedding 服务挂了**降级不炸**（demo 行为） | 无（A5 票再上前端） |
| **4** | `base.process` | 阶段推进引擎：主链定义解析 / next() 迁移 / 驳回停留 / 门禁计数（minTasks 按阶段可配，A3：验收段 = 0）；**无表**——状态由持有方（business.project 的期聚合，A3 修订）保存 | — | 单测全覆盖：推进 / 驳回停留 / 门禁不足拒绝 / 主链末尾 DONE | 无 |
| **5** | `business.project` 最小版 | `Project` 聚合（业务字段 + workspaceId；stage/计数在 `Iteration`，A3 修订）+ 期/确认/需求池实体（A3：`prj_iterations` / `prj_confirmations` / `prj_demand_pool_entries`）+ 角色卡 preset（六角色 BA/DEV/DELIVERY/ARCH/TEST/DEMO，代码配置不落库）+ 阶段→产物文件名映射 + 主链 Controller 总装（demo `ProjectController`+`AgentController` 的重写：projects / agent task / questions / permission / preview / stage / approve / reject / delete）+ EventHub 事件桥接 | `prj_projects` · `prj_iterations` · `prj_confirmations` · `prj_demand_pool_entries` | **全链路五步冒烟**（见 §5 checklist） | **发**：主链页面对接 localhost:8888 |

---

## 3. 弱化实现与演化升级方向

| 现状（Phase A 弱化版） | 触发信号 | 升级路径（参考出处） |
|---|---|---|
| docker CLI 子进程（ProcessBuilder） | 上云 / 多机（Phase B） | `EnvironmentBackend` 端口缝新增 TKE/远端适配器，配置切换（demo `EnvironmentConfig` 已预留） |
| SSE 内存单实例、重启丢事件 | 多实例部署 / 事件可靠性要求 | 事件持久化（`agent_run_events`，`{runId}:{seq}` 复合 ID）先于广播 + Last-Event-ID 断线补发；CC `docs/design/04` §4.6、`EventReplayService.java` 有实现参考 |
| JdbcTemplate + pgvector | 数据量 / 检索质量需求 | `KnowledgePort` 换腾讯云向量库（>100 万向量才迁，MVP 不引入既定）；fog 已记 embedding 生产形态 |
| 角色卡代码配置 | 角色多了要运营管理 | preset 落库（可热更新，CC `agent_roles` 表参考）+ **模型档位路由**：角色卡存档位（reasoning/coding/fast）不存模型名，`llm_providers.model_tiers` 做映射，换模型不动角色卡（CC `docs/design/07` §7.2） |
| agent 会话最小落库 | 多副本 / 高可用 | 会话与运行状态外置存储（CC 用 PG AgentStateStore 多副本就绪） |
| 单体同进程 | 规模到了要拆服务 | 端口即边界，换 REST/消息实现，业务域不动（docs/11 既定） |

---

## 4. Code-Canvas 参考分流表

### ① 已吸收进本蓝图（零实现成本的设计前瞻）

| 吸收点 | 落点 |
|---|---|
| 阶段闸门 vs 工具级 HITL **双层分离**（两张表、一个 UI 入口） | 佐证 docs/11「门与 HITL 不统一建模」；写进 §1 process/agentengine 边界注记与 CONTEXT.md 关键边界 |
| SSE 事件 ID 用 `{runId}:{seq}` 格式 | 片 2 事件规范留缝：持久化补发 Phase A 不做，但 **ID 格式从第一天留好** |
| 验收「副作用以**真实状态**（文件/git/DB）为准，非事件自述」 | §5 验收口径总则 |
| 「未实测假设全部进 PoC 清单（≤半天）+ 预写备选」纪律 | §5 验收口径总则 |
| 消息表 / AgentState 双轨存储纪律（用户可见真相 ≠ 智能体恢复状态；平台代码不读写框架存储内部结构） | 片 5 数据模型注记 |

### ② 挂到既有票做输入（不扩大 Phase A）

| 参考点 | 出处 | 挂靠 |
|---|---|---|
| Checkpoint：每轮任务 = git commit，回滚默认 revert 保历史 | CC `docs/design/04` §4.4、`05-数据模型.md` | fog「产物版本化与回退」（地图 Not-yet-specified） |
| 计量三类（llm_tokens / sandbox_seconds / storage_bytes）+ 项目级配额（0=封停、run 起跑前拦截） | CC `docs/design/07` §7.3、`09` M4-B 补记 | A6 计量服务票（#12） |
| 人类角色授权矩阵（owner/developer/tester，服务端切面强制，「前端隐藏按钮不算权限控制」） | CC `06-API契约.md` §6.3、`AuthzService.java` | A2 账号票（#6） |
| HITL 避雷清单 5 条：权限上下文随会话持久化致旧会话 ASK 不生效、拒绝后 LLM 重试成审批循环、AgentState 存本地文件路径、工作区外绝对路径逃逸、工具超时留 ASKING 死状态 | CC `.agents/memory/agentscope-hitl.md`、`agentscope-harness-runtime.md` | A1 四口子票（#5） |

### ③ 明确避开

- AgentScope Java 绑定（MVP 不引入，既定决策）
- Node 遗留物（`artifacts/api-server`、`replit.md` 双栈混杂段）
- pgvector 设计稿（CC 只有设计无实现；demo 有实现且更强）

---

## 5. 验收口径总则

1. **对照对象是本服务自己**，不是 demo——demo 的唯一用途是实现时查它怎么解决的，不对拍。
2. **副作用以真实状态为准**（文件 / git / DB / 容器存活），不以事件自述为准。
3. 未实测的假设（第三方库行为、CLI 输出格式、超时语义等）**全部进 PoC 清单**：每个 ≤半天可验证，且预写备选方案。
4. cartisan 测试规范照常执行（领域层 95%+ / 应用层 90%+ / 基础设施 80%+；`given_when_then` 命名）。
5. 单片验收 = 该片 REST/SSE 端点手动 curl 可用 + 测试过；全链路验收 = 片 5 完成时走 §6。

## 6. 全链路五步冒烟 checklist（片 5 验收）

前置：`GET /api/events` SSE 长连接已建立；本机 Docker daemon、平台 PG（含 vector 扩展）、Redis、embedding 服务（:9091）就绪。

| # | 步骤 | 动作 | 通过标准（看真实状态） |
|---|---|---|---|
| 1 | 建项目 | `POST /api/projects`（选引擎） | dev 容器 + 项目专属 pg/redis 容器存活；`/workspace/.env` 已写入连接串；`wsp_*`/`prj_projects` 落库；SSE `workspace-created` + `stage-changed`(BA) |
| 2 | 下任务 | `POST /api/projects/{id}/agent/task` | SSE 依次 `role-assigned` →（`knowledge-retrieved`）→ `task-start` → `session-created` → part 事件流 → `task-finish`；`agt_agent_sessions` 落库；taskCount=1 |
| 3 | HITL | `GET …/agent/questions` → `POST …/agent/questions/reply`；`POST …/agent/permission` | agent 收到答复继续跑完；权限拒绝不形成审批循环 |
| 4 | 预览 | `GET /api/projects/{id}/preview` | 返回可访问的 localhost 预览 URL；SSE `preview-ready` |
| 5 | 审批流转 | `POST …/stage/approve`（至验收门） | 每门：taskCount≥1 才放行（验收门 = 0）；本阶段产物分块入 `knw_*` 且 embedding 就位；stage 推进、SSE `stage-changed`；门不足 409；驳回停留（带 reason）；**验收门通过即收口：期 CLOSED、项目已交付**（A3：无交付段） |
| 6 | 收尾 | `DELETE /api/projects/{id}` | 容器/网络/卷级联清理；三区表记录删除；SSE `workspace-destroyed` |

（步骤编号沿用五步口径，「收尾」为顺带验证。）

---

## 附：与地图的关系

- 本蓝图 = 地图票 [B0 · 底座重写切片蓝图](https://github.com/ZhangColin/aiplatform-server/issues/2) 的产出；后续每片走既有 grill→spec→ticket→implement 路径，不再开 wayfinder 票。
- 片 2 spec 前置：地图 [API 与 SSE 事件规范](https://github.com/ZhangColin/aiplatform-server/issues/3) 票先定稿。
- A1（四口子）/ A2（账号）/ A3（期状态机映射）/ A6（计量）各票的 Code-Canvas 输入指针见 §4②。

## 修订记录

- **修订 A3（2026-08-20，[票 #9](https://github.com/ZhangColin/aiplatform-server/issues/9)）**：主链 = 平台业务过程（**「模板」概念退役**，主链定义唯一一条由业务侧传入）；**状态机主体从 Project 迁至 `prj_iterations`**（期），新增 `prj_confirmations`（确认留痕，append-only）/ `prj_demand_pool_entries`（需求池）；验收门通过即收口（无交付段）；门禁 minTasks 按阶段可配（验收 = 0）、产物清单只作沉淀范围不作门禁。详见 [A3 · 主链规格](A3-主链规格.md)。
- **修订 A2（2026-08-20，[票 #6](https://github.com/ZhangColin/aiplatform-server/issues/6)）**：business 分区补 **identity**（账号 + OIDC BFF 认证，表 `idn_accounts`，错误码 `IDN_`）与 **workbench**（查询侧聚合，无表）两 BC；`prj_projects` 增 `owner_account_id` 列（创建时填、v1 不过滤）。详见 [A2 · 账号与工作台骨架规格](A2-账号与工作台骨架规格.md)。
- **修订 A1（2026-08-20，[票 #5](https://github.com/ZhangColin/aiplatform-server/issues/5)）**：base 分区五 BC → **六 BC**——新增 `base.metering`（计量上下文平台内起步：UsageEventSink 端口 / `met_usage_events` / UsageQueryPort，错误码前缀 `METER_` 用 ADR-0001 已预留位）。**片 1** 范围补：环境生命周期应用事件发布端（WorkspaceCreated / WorkspaceDestroyed / PreviewReady，cartisan 应用事件）。**片 2** 范围补：等待点表 `agt_pending_waits`、`runTask` 支持复用 sessionId（续跑缝）与 UsageContext 透传、run 级 UsageEvent 埋点（opencode）。详见 [A1 · 底座四口子规格](A1-底座四口子规格.md)。
